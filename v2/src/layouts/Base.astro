---
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  noindex?: boolean;
}

const {
  title = 'Old Forest Echoes',
  description = 'Music and art composed in Finnish old growth forests. A project by Barbora Xu in collaboration with the Finnish Nature League, aspiring to raise awareness and protect threatened forests.',
  ogImage = '/images/forest-bg.webp',
  noindex = false,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  {noindex ? <meta name="robots" content="noindex" /> : <meta name="robots" content="index, follow" />}
  <link rel="canonical" href={canonicalURL} />
  <meta name="theme-color" content="#0a0a08" />
  <link rel="icon" href="/logo.svg" type="image/svg+xml" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
  <link rel="sitemap" href="/sitemap-index.xml" />

  <!-- Open Graph -->
  <meta property="og:type" content="website" />
  <meta property="og:locale" content="en_US" />
  <meta property="og:url" content={canonicalURL} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={new URL(ogImage, Astro.site)} />
  <meta property="og:site_name" content="Old Forest Echoes" />

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={new URL(ogImage, Astro.site)} />

  <!-- Fonts (self-hosted, no third-party requests) -->
  <link rel="preload" href="/fonts/Leonetta-Serif.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="/fonts/Montserrat.woff2" as="font" type="font/woff2" crossorigin />

  <!-- Preload LCP background image -->
  <link rel="preload" href="/images/forest-bg.webp" as="image" type="image/webp" fetchpriority="high" />
</head>
<body>
  <div class="page-background" style="background-image: url('/images/forest-bg.webp')"></div>
  <div class="page-overlay"></div>
  <a href="#main-content" class="skip-link">Skip to content</a>
  <Nav />
  <main id="main-content" class="container">
    <slot />
  </main>
  <Footer />
</body>
</html>
