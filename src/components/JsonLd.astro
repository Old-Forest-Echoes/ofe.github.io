---
interface Props {
  data: Record<string, unknown> & { '@context': string; '@type': string };
}

const { data } = Astro.props;
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(data).replaceAll('&', '\\u0026').replaceAll('<', '\\u003c').replaceAll('>', '\\u003e')} />
